angular.module('gameApp')
.controller( 'navBarCtrl', function( $scope, resultsSrv, $firebaseArray, fb ){
	// $scope.showFavorites = false;

	// var userFavs = new Firebase( fb.urlRef + "/users/" + resultsSrv.userId + "/favorties");
	// $scope.favorites = $firebaseArray( userFavs);
	// console.log($scope.favorites);
})